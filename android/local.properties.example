# Copy this file to local.properties (never commit local.properties).
# Set the backend URL your Android device/emulator can reach.
# For the emulator, 10.0.2.2 is the host machine's localhost.
FUNMAP_BACKEND_URL=http://10.0.2.2:8080/
